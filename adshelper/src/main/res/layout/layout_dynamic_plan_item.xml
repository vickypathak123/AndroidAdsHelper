<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/plan_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- This is the content of your old "layPlan1" -->
    <include
        android:id="@+id/ly_plan_main"
        layout="@layout/two_plan_main_item"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_12sdp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <include
        android:id="@+id/ly_plan_tag"
        layout="@layout/two_plan_tag_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/_25sdp"
        app:layout_constraintBottom_toTopOf="@id/ly_plan_main"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/ly_plan_main" />

</androidx.constraintlayout.widget.ConstraintLayout>